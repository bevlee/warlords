apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: warlords-data
  namespace: warlords
  labels:
    app: warlords
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi

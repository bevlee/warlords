# Skill icons

Drop a PNG named after the skill id here (e.g. `double_strike.png`,
`bravery.png`) and `src/lib/ui/skillIcons.ts` picks it up automatically.
Until then the UI shows an emoji glyph fallback.
